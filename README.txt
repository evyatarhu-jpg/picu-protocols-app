PICU / PCICU Protocols — Full v2

This clean build contains all protocol HTML pages and all source documents as PDFs.
Source PDFs are stored under: originals/

Open index.html with Live Server.
All protocol cards should open their corresponding HTML page.
Each "מסמך מקור" button opens a PDF source document in a new tab.

The service worker uses network-first navigation for HTML pages to reduce stale-cache issues during updates.
